# validation of this batch
1. login validation: 

```text
you can log in
your user name appears correctly
logout/login works if you want stronger check
```

2. Admin/security pages

open pages:

```text
Manage Jenkins
Manage Jenkins -> Security
Manage Jenkins -> Credentials
Manage Jenkins -> In-process Script Approval
Manage Jenkins -> Plugins
```

3. Credentials validation

```text
Manage Jenkins -> Credentials
```

Check important credentials still exist, for example:

```text
jenkinsbb
bitbucket-global
bb-jenkins-userpwd
```

4. Permission validation

Open a few existing jobs/folders:

nasekatest
plugin-check-workflow-durable-task-step
one existing multibranch job

        job page opens
        Configure/View Configuration opens if your role should allow it
        Build Now is visible where expected
        no unexpected Access Denied

5. Script approval validation

Manage Jenkins -> In-process Script Approval

page opens
approved/pending scripts are visible
no errors


6. Functional smoke

run:

built-in Pipeline smoke
nasekatest multibranch build


success! 77 updates left