```bash
${name// /}
```

`//` means replace all occurences

`/` replace first occurence

` ` this is the pattern to match 

`/` there is nothing after second / -> so matched spaces are replaced with nothing